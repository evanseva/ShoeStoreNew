<Window x:Class="ShoeStore.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в систему" Height="420" Width="400"
        WindowStartupLocation="CenterScreen"
        FontFamily="Times New Roman" FontSize="14"
        ResizeMode="NoResize">

    <StackPanel Margin="30" VerticalAlignment="Center">

        <TextBlock Text="Добро пожаловать!" 
                   FontSize="22" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,5"/>

        <TextBlock Text="Введите данные для входа" 
                   FontSize="12" Foreground="Gray"
                   HorizontalAlignment="Center" Margin="0,0,0,30"/>

        <TextBlock Text="Логин:" Margin="0,5" FontWeight="Bold"/>
        <TextBox Name="txtLogin" Height="35" Margin="0,0,0,15" Padding="8"/>

        <TextBlock Text="Пароль:" Margin="0,5" FontWeight="Bold"/>
        <PasswordBox Name="txtPassword" Height="35" Margin="0,0,0,25" Padding="8"/>

        <Button Content="ВОЙТИ" 
                Background="#00FA9A" Foreground="Black" FontWeight="Bold"
                Height="40" Click="BtnLogin_Click"
                Cursor="Hand" Margin="0,0,0,10"/>

        <Button Content="ПРОДОЛЖИТЬ КАК ГОСТЬ" 
                Background="#7FFF00" Foreground="Black"
                Height="35" Click="BtnGuest_Click"
                Cursor="Hand" Margin="0,0,0,20"/>

        <TextBlock Text="Тестовые логины: admin/admin, manager/manager, client/client" 
                   FontSize="9" Foreground="Gray" TextWrapping="Wrap"
                   HorizontalAlignment="Center"/>
    </StackPanel>
</Window>